This is an Ai/ML project that Performs Gold Price Prediction built using Jupyter Notebook.
The Model is trained on data to provide predictions.
It uses linear Regression And Random Forest Regressor.
It uses Libraries such as Pandas ,Matplotlib ,Numpy.
It runs on Localhost But the Model should be Runned
when Its Live.
